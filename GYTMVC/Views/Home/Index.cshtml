@model List<ENT.Concrete.Rank>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>
        @foreach (var item in Model)
        {
            @item.RankName <br />
        }
    </p>
</div>
